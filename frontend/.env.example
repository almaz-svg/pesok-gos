# Public frontend configuration only. Never put server secrets here.
# Copy to frontend/.env; Vite reads environment files from this directory.
# demo: fictional browser data; api: Django through the Vite /api proxy.
VITE_DATA_MODE=demo
VITE_API_BASE_URL=/api
